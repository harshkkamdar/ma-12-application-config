# application.properties (Default Configuration)
# Default configuration for the application

# Property for application name
application.name=Harsh's App

# Database configuration
database.url=jdbc:mysql://localhost:3306/default_db
database.username=root
database.password=root_password

# Log level configuration
logging.level.root=INFO
logging.level.com.example=DEBUG

# API base URL
api.base.url=https://api.default.com

# Some feature flag
feature.someFeatureEnabled=true

# Other default configurations
some.property=Default Configuration
